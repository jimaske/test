#include<stdio.h> 
#include<malloc.h>
#include<string.h>
char* StrCpy(char* src)
{
	int i = 0;
	int len = strlen(src);
	char *p = NULL;
	p = (char*)malloc(sizeof(char) * len);
	for (i = 0; i < len; i++) {
		p[i] = src[i];
	}
	p[i] = '\0';
	free(p);
	return p;
}
